The following is a game made in Rust with future plans to have a UI built in react.js allowing the game to be played in a web browser.
The idea is to make the UI look like an email client but actually be a largely text based game.

